{% extends ('base.html.twig') %}
{% block title %}Reseting password{% endblock %}
{% block body %}

    <div class="container">

    </div>
    <div class="container">
        <div class="row mt-5">
            <div class="col-md-2 col-1"></div>
            <div class="col-md-8 col-10">
                <div class="bg-light py-5 px-3">
                    <h1>Renouvellement du mot de passe</h1>
                    {{form_start(form)}}
                    {{form_row(form.hash.first)}}
                    {{form_row(form.hash.second)}}
                    <button class=""  type="submit">Envoyer</button>
                    {{form_end(form)}}
                </div>
            </div>
        </div>
    </div>

{% endblock %}